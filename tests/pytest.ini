[pytest]
markers =
    unit: unit tests (fast, isolated components)
    integration: integration tests (slower, with data flow)
    e2e: end-to-end workflow tests
    gpu: tests that may use GPU
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
